[project]
name = "sigmazero"
version = "0.1.0"
description = "Zero volatility portfolio allocation subnet"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "affinetes",
    "aiohttp",
    "bittensor>=8.0.0",
]
